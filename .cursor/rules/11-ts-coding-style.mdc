---
description: TypeScript coding style — types, immutability, Zod, errors
globs: "**/*.{ts,tsx}"
alwaysApply: false
---

# TypeScript/JavaScript Coding Style

## Types and Interfaces

### Public APIs

- Add parameter and return types to exported functions, shared utilities, and public class methods
- Let TypeScript infer obvious local variable types
- Extract repeated inline object shapes into named types or interfaces

```typescript
// WRONG: Exported function without explicit types
export function formatUser(user) {
  return `${user.firstName} ${user.lastName}`
}

// CORRECT: Explicit types on public APIs
interface User {
  firstName: string
  lastName: string
}

export function formatUser(user: User): string {
  return `${user.firstName} ${user.lastName}`
}
```

### Interfaces vs. Type Aliases

- Use `interface` for object shapes that may be extended or implemented
- Use `type` for unions, intersections, tuples, mapped types, and utility types
- Prefer string literal unions over `enum` unless an `enum` is required for interoperability

### Avoid `any`

- Avoid `any` in application code
- Use `unknown` for external or untrusted input, then narrow it safely
- Use generics when a value's type depends on the caller

### React Props

- Prefer `type Props = {}` for closed component prop shapes
- Use `interface` when the prop type is an exported extension point
- Do not use `React.FC` unless there is a specific reason
- Always destructure props in the parameter list

## Immutability

```typescript
function updateUser(user: Readonly<User>, name: string): User {
  return { ...user, name }
}
```

## Error Handling

Use async/await with try-catch and narrow `unknown` errors safely. Prefer structured logging over `console.log` in production paths.

## Input Validation

Use Zod for schema-based validation and infer types from the schema:

```typescript
import { z } from "zod"

const userSchema = z.object({
  email: z.email(),
  age: z.number().int().min(0).max(150),
})

type UserInput = z.infer<typeof userSchema>
```

Share Zod schemas between TanStack Form validators and tRPC procedure inputs.

## Console.log

- No `console.log` statements in production code
- Temporary debug logs must be removed before commit
